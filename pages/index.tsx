import { Loader, ProjectCard } from 'components';

import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <>
      <Loader onFinish={() => {}} />
    </>
  );
};

export default Home;
